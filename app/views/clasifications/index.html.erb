<h1>Listing clasifications</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Description</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @clasifications.each do |clasification| %>
  <tr>
    <td><%= clasification.name %></td>
    <td><%= clasification.description %></td>
    <td><%= link_to 'Show', clasification %></td>
    <td><%= link_to 'Edit', edit_clasification_path(clasification) %></td>
    <td><%= link_to 'Destroy', clasification, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Clasification', new_clasification_path %>
